
- Use pydantic to validate config?
  - Downside is additional dependency

- Add check for include to make sure the same file isn't include multiple times

- Additional documentation, man page

- Add type hints

- Add option to override checksum on command line

- Treat kickstart as a template if file is not found? Or add kickstart template option?

- Allow overriding grub_cfg_path

- Detect loops in include logic

- Allow lone docstrings in flavor configuration

- Allow overrides for implantisomd5 and xorrisofs?

- Add option to overwrite/provide volume_id on command line
  - Avoid using option '-v' since it's often associated with verbosity
